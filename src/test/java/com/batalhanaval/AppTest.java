package com.batalhanaval;

import org.junit.jupiter.api.Test;
import static org.junit.jupiter.api.Assertions.assertTrue;

/**
 * Unit tests for the App class.
 */
public class AppTest {
    /**
     * Simple test to verify the test environment is correctly configured.
     */
    @Test
    public void testAppInitialization() {
        // Simple test that always passes - just to verify the environment is working
        assertTrue(true);
    }
}
